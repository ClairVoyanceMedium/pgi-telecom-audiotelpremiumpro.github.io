{
  "name": "pgi-telecom-audiotel-premium-pro",
  "version": "1.0.0",
  "private": true,
  "description": "PGI Telecom • Audiotel Premium Pro — dashboard SVA/Audiotel",
  "scripts": {
    "check:js": "node --check assets/config.js && node --check assets/app.js && node --check service-worker.js",
    "check:static": "node scripts/check-static.mjs",
    "check:secrets": "node scripts/check-secrets.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check:js && npm run check:static && npm run check:secrets && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
